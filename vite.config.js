import { defineConfig } from 'vite';

export default defineConfig({
  // root: '.',        // default is project root
  build: {
    outDir: 'dist',    // default
    // assetsDir: 'assets',
  },
  // resolve: {
  //   alias: { '@': '/src' }
  // }
});
